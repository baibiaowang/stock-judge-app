{
  "name": "stock-judge-app",
  "version": "1.0.0",
  "private": true,
  "description": "Stock announcement judge viewer - Android APK with encrypted data",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "inject": "node native/inject.mjs",
    "patch": "node native/patch-manifest.mjs",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
